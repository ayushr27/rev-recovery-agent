LLM_PROVIDER=groq
GROQ_API_KEY=
# Optional. Defaults to openai/gpt-oss-20b, which produced every committed cache entry and
# every quoted figure — change it and those figures no longer describe what you are running.
GROQ_MODEL=
GEMINI_API_KEY=
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
